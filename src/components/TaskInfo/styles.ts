import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  taskInfoWrapper: {
    flexDirection: "row",
    gap: 107,
    alignItems: "center",
    maxHeight: 25,
    marginTop: 34,
  },
});
